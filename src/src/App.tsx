import * as React from 'react';

interface AppProps {

};
interface AppState  {

};

export class App extends React.Component<AppProps, AppState> {
    render() {
        return (
            <div>
                Hi there!
            </div>
        );
    };
}
